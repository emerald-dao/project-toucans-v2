<span><slot /></span>

<style lang="scss">
	span {
		text-align: center;
		font-size: var(--font-size-3);
		line-height: 19px;
		letter-spacing: 0.1em;
		text-transform: uppercase;
		background: -webkit-linear-gradient(var(--clr-text-main), var(--clr-heading-main));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
	}
</style>
